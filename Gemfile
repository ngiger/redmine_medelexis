source 'http://rubygems.org'

gem 'minitest-reporters'
gem  "xml-simple"
# gem  "simplecov", "~> 0.9.1"
gem  "yaml_db"
group 'debugger' do
  gem (RUBY_VERSION >= "2.0" ? 'pry-byebug' : 'debugger')
end
