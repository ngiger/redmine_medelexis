source 'https://rubygems.org'

gem 'minitest-reporters'
gem  "xml-simple"
gem  "yaml_db"
gem 'bigdecimal', '1.3.5'
group 'debugger' do
  gem 'pry-byebug' if RUBY_VERSION >= "2.4"
  gem 'pry-byebug', "3.5.1" if RUBY_VERSION < "2.4" && RUBY_VERSION >= "2.2"
end
